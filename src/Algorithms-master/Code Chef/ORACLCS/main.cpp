#include<iostream>
#include<stdio.h>
#include<string.h>
using namespace std;


int main()
{
    int t,n,i,j,m,lcr;
    cin>>t;
    while(t--)
    {
        cin>>n;
        char str[n][100];
        lcr=1;
        for(i=-1;i<n;i++)
            cin.getline(str[i],100,'\n');
            //PUTCODEHERE
    }
    return 0;
}